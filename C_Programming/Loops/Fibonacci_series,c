#include<stdio.h>
int main()
{
    int n,i,a =1,b=1,next;
    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        if(i==1||i==2)
            printf("1 ");
        else
        {
            next=a+b;
            printf("%d ", next);

            a=b;
            b=next;
        }
    }


    return 0;
}
